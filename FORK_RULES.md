# Fork-specific rules

Add rules here that apply only to this fork. Keep the shared guidance in
`AGENTS.md`, `AUTHORING_GUIDE.md`, and `CLAUDE.md` synchronized with the
`ea-script-template` master repository so upstream updates can be merged cleanly.

This file is intentionally a local extension point. Replace or extend this
placeholder with the conventions, integrations, and publishing details that
are unique to your fork.
