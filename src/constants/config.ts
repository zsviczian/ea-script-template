/**
 * @file config.ts
 * @overview Numeric and boolean configuration constants.
 *   Keeping magic numbers out of logic keeps functions readable.
 */

/** Default stroke width for newly created elements. */
export const DEFAULT_STROKE_WIDTH = 2;

/** Default font size for text elements. */
export const DEFAULT_FONT_SIZE = 20;

/** Maximum elements this script will process in a single run. */
export const MAX_ELEMENTS = 500;
